diff --git a/dist/shared-CmTSrQGO.mjs b/dist/shared-CmTSrQGO.mjs
index c3889430c62764db69edf1fa68c89b6ed788b883..550b6746ab6b07b2c912efa207c1d043305b2695 100644
--- a/dist/shared-CmTSrQGO.mjs
+++ b/dist/shared-CmTSrQGO.mjs
@@ -70,7 +70,10 @@ function MarkdownItLink(md) {
 const RE_WHITESPACE_ONLY = /^\s*$/;
 const RE_QUOTED_STRING = /^(['"])(.*)\1$/;
 const RE_WHITESPACE = /\s+/g;
-const RE_WINDOWS_DRIVE = /^[A-Z]:\//i;
+const RE_WINDOWS_DRIVE = /^\/?[A-Z]:\//i;
+function normalizeWindowsDrivePath(path$1) {
+	return slash(path$1).replace(/^\/([A-Z]:\/)/i, "$1");
+}
 let jiti;
 function loadModule(absolutePath) {
 	jiti ??= createJiti(fileURLToPath(import.meta.url), { moduleCache: false });
@@ -133,13 +136,14 @@ function getBodyJson(req) {
 	});
 }
 function getImportGlobRelativePath(from, to) {
-	const normalizedFrom = slash(from);
-	const normalizedTo = slash(to);
+	const normalizedFrom = normalizeWindowsDrivePath(from);
+	const normalizedTo = normalizeWindowsDrivePath(to);
 	return slash(RE_WINDOWS_DRIVE.test(normalizedFrom) || RE_WINDOWS_DRIVE.test(normalizedTo) ? win32.relative(normalizedFrom, normalizedTo) : relative(normalizedFrom, normalizedTo));
 }
 function makeAbsoluteImportGlob(self, globs, options = {}, baseRoot) {
-	const root = baseRoot && globs.some((glob) => RE_WINDOWS_DRIVE.test(slash(glob))) ? baseRoot : void 0;
+	const root = baseRoot && globs.some((glob) => RE_WINDOWS_DRIVE.test(slash(glob))) ? normalizeWindowsDrivePath(baseRoot) : void 0;
 	const relativeGlobs = globs.map((glob) => {
+		glob = normalizeWindowsDrivePath(glob);
 		const relativeGlob = getImportGlobRelativePath(root ?? self, glob);
 		return root && !relativeGlob.startsWith(".") && !RE_WINDOWS_DRIVE.test(relativeGlob) ? `./${relativeGlob}` : relativeGlob;
 	});
@@ -1250,11 +1254,21 @@ const templateConditionalStyles = {
 	id: id$1,
 	async getContent({ data, roots, userRoot }) {
 		const imports = [];
-		for (const root of roots) imports.push(makeAbsoluteImportGlob(id$1, [
-			join(root, "styles/index.{ts,js,css}"),
-			join(root, "styles.{ts,js,css}"),
-			join(root, "style.{ts,js,css}")
-		], {}, userRoot));
+		for (const root of roots) {
+			for (const file of [
+				join(root, "styles/index.ts"),
+				join(root, "styles/index.js"),
+				join(root, "styles/index.css"),
+				join(root, "styles.ts"),
+				join(root, "styles.js"),
+				join(root, "styles.css"),
+				join(root, "style.ts"),
+				join(root, "style.js"),
+				join(root, "style.css")
+			]) {
+				if (existsSync(file)) imports.push(`import "${toAtFS(file)}"`);
+			}
+		}
 		if (data.features.katex) imports.push(`import "${await resolveImportUrl("katex/dist/katex.min.css")}"`);
 		return imports.join("\n");
 	}
@@ -1405,10 +1419,21 @@ function createSetupTemplate(name) {
 	const id$2 = `/@slidev/setups/${name}`;
 	return {
 		id: id$2,
-		getContent({ roots, userRoot }) {
-			return `export default [${roots.map((root) => {
-				return `Object.values(${makeAbsoluteImportGlob(id$2, [join(root, `setup/${name}.{ts,js,mts,mjs}`)], { import: "default" }, userRoot)})[0]`;
-			}).join(", ")}].filter(Boolean)`;
+		getContent({ roots }) {
+			const imports = [];
+			const setups = [];
+			for (const root of roots) {
+				for (const ext of ["ts", "js", "mts", "mjs"]) {
+					const file = join(root, `setup/${name}.${ext}`);
+					if (existsSync(file)) {
+						const index = imports.length;
+						imports.push(`import __setup_${index} from "${toAtFS(file)}"`);
+						setups.push(`__setup_${index}`);
+						break;
+					}
+				}
+			}
+			return `${imports.join("\n")}\nexport default [${setups.join(", ")}].filter(Boolean)`;
 		}
 	};
 }
